import { render } from "@testing-library/svelte";
import Statuses from "./Statuses.svelte";

describe("Statuses", () => {
  it("should", () => {
    render(Statuses);
    expect(0).toBe(0);
  });
});

export {};
